<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="layout.pedidosaceptados">

    <ListView
        android:id="@+id/listaceptado"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:choiceMode="singleChoice"
        android:background="#ffffff"
        android:dividerHeight="0.5sp"/>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/lblEstadoaceptado"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:src="@drawable/logoflash"
            android:visibility="gone"/>

        <TextView
            android:layout_below="@+id/lblEstadoaceptado"
            android:id="@+id/lblmensajeaceptado"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="No tienes pedidos aceptados"
            android:layout_centerHorizontal="true"
            android:textSize="20sp"
            android:textColor="#9E9E9E"
            android:layout_marginTop="10sp"
            android:visibility="gone"
            />

    </RelativeLayout>

</FrameLayout>
